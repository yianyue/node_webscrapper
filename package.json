{
  "name": "webScrapper",
  "version": "1.0.0",
  "description": " a command line program that can parse remote pages, read the html, and write the content that we care about to a file on disk.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yian Yue",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.19.0",
    "request": "^2.60.0"
  }
}
